# ================================
# データベース接続
# ================================
# 開発環境（Docker MySQL）
DATABASE_URL="mysql://root:root@localhost:3306/nippo"

# 本番環境の例（Google Cloud SQL）
# DATABASE_URL="mysql://user:password@/nippo?host=/cloudsql/project:region:instance"

# テスト環境の例（SQLite）
# DATABASE_URL="file:./test.db"

# ================================
# JWT認証
# ================================
# 本番環境では必ず強力なランダム文字列に変更してください
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secret-key-here-please-change-in-production-use-long-random-string"
JWT_REFRESH_SECRET="your-refresh-secret-key-here-please-change-in-production-use-long-random-string"

# トークンの有効期限
JWT_EXPIRES_IN="1h"           # アクセストークン: 1時間
JWT_REFRESH_EXPIRES_IN="30d"  # リフレッシュトークン: 30日

# ================================
# Next.js
# ================================
# クライアントサイドでアクセス可能なAPI URL（NEXT_PUBLIC_プレフィックス必須）
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# 本番環境の例
# NEXT_PUBLIC_API_URL="https://your-domain.com/api"

# ================================
# 環境
# ================================
NODE_ENV="development"  # development | production | test

# ================================
# オプション設定
# ================================
# ログレベル（開発環境のみ）
# LOG_LEVEL="debug"  # debug | info | warn | error

# レート制限（本番環境推奨）
# RATE_LIMIT_MAX="1000"  # 1時間あたりの最大リクエスト数

# CORS設定（本番環境で必要に応じて設定）
# CORS_ORIGIN="https://your-frontend-domain.com"

# ================================
# Google Cloud Run設定
# ================================
# Cloud Run用のポート（デフォルト: 8080）
# PORT="8080"

# Cloud SQLプロキシ接続（Unix Socket接続）
# CLOUD_SQL_CONNECTION_NAME="project-id:region:instance-name"
# DATABASE_URL="mysql://user:password@localhost/nippo?socket=/cloudsql/${CLOUD_SQL_CONNECTION_NAME}"

# GCP プロジェクトID
# GCP_PROJECT_ID="your-project-id"

# Cloud Runサービス名
# CLOUD_RUN_SERVICE="nippo-app"

# Cloud Runリージョン
# CLOUD_RUN_REGION="asia-northeast1"
